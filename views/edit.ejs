<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full bg-zinc-900 flex items-center justify-center">
    <div class="flex flex-col gap-10">
        <!-- Edit User Section -->
        <div class="p-6 rounded-lg">
            <h1 class="text-white px-5 text-2xl font-bold mb-4">Edit User</h1>
            <form action="/update/<%= user._id %>" method="POST" class="flex gap-4">
                <input type="text" name="name" value="<%= user.name %>" class="px-4 py-2 rounded-md bg-zinc-800 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                <input type="email" name="email" value="<%= user.email %>" class="px-4 py-2 rounded-md bg-zinc-800 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                <input type="text" name="image" value="<%= user.image %>" class="px-4 py-2 rounded-md bg-zinc-800 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                <button type="submit" class="px-6 py-2 bg-yellow-500 text-white rounded-md hover:bg-blue-600">Update User</button>
            </form>
        </div>
    </div>
</body>
</html>
