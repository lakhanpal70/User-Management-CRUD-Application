<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 mt-3" href="/"> Back to home</a>
      <h1 class="text-white mt-5 text-2xl font-bold mb-4">All User</h1>
      <br>
      <div class="users flex gap-3 flex-wrap">
        <% if(users.length > 0) { %>
            <% users.forEach(function(user){ %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter"> <%= user.name %></h3>
                    <h5 class="text-zinc-500"> <%= user.email %></h5>
        
                    <div class="flex gap-3 mt-10">
                        <a href="/edit/<%= user._id %>" class="text-zinc-200">Edit User</a>
     <!-- Corrected Delete link with user._id -->
            <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>                </div>
                </div>
            <% }) %>

        <%} else { %>
            <h3 class="text-zinc-500">No User Yet</h3>
            <%

        } %>
        
    </div>
    
    </div>
</body>
</html>
